# MathStick — Project Knowledge

> Single reference for the MathStick mobile game: what it is, how it is built,
> how the economy works, and the decisions behind both. Written to be dropped
> into a Claude Project as knowledge.
>
> Last updated: 2026-08-20

---

## 1. What the game is

A mobile puzzle game where arithmetic is drawn with **seven-segment matchsticks**.
The player is shown an equation with segments missing and must tap segments to
form digits that make the equation true.

```
   ┌─┐       ┌─┐
   │ │  ...  │ │      ← tap segments to build each digit
   ├─┤   +   ├─┤
   │ │       │ │
   └─┘       └─┘
   ─────────────
      answer
```

Two modes:

| Mode | Shape | Cost | Purpose |
|---|---|---|---|
| **TRAINING** | Single player, endless streak, own difficulty pick | Free forever | Learning, practice, the guaranteed fallback |
| **ARCADE 1v1** | Real-time race, first-to-N rounds | Entry fee in coins | Competition, rating, retention |

**The 1v1 rule:** both players get the *same* equation at the same moment. The
round ends the instant either one answers correctly. There is **no time limit** —
it is a race, not a countdown. Both players are then shown the correct answer.

---

## 2. Identity and accounts

| Thing | Value |
|---|---|
| Unity version | **2022.3.0f1** |
| Project path | `D:\JOC\PlusMinus_Unity\PlusMinus_Unity` (note the doubled folder) |
| Android package | `com.JOC.PlusMinus` |
| Firebase project | `plusminus-46df9` |
| Realtime DB region | `europe-west1` (`plusminus-46df9-default-rtdb`) |
| Firebase plan | **Spark (free)** — no Cloud Functions available |
| GitHub | `https://github.com/obretingeorge09/MathStick` |
| Facebook app ID | `1409681074530831` |
| Google web client ID | `635048943585-907cr1g7a50havpfmc6hv8akm1q4kck0.apps.googleusercontent.com` |

**Platform status:** Android only. iOS is blocked — no Mac and no Apple Developer
account. Nothing in the codebase prevents iOS; it is purely a tooling gate.

**Scale:** pre-launch. **Zero players.** Every design decision below is weighted
for that fact — mechanics that work at a million users often kill a game at zero.

---

## 3. The one convention that governs everything

> **The entire UI is generated by an Editor script. Do not edit the scene by hand.**

`Assets/Editor/SceneBuilder.cs` (~3,100 lines) builds every panel, button, label
and sprite procedurally and saves `Assets/Scenes/MainScene.unity`.

**To apply any UI change: Unity menu → `PlusMinus` → `Build Scene`.**

Consequences that trip people up:

- Hand-edits to the scene are destroyed on the next build.
- Sprites are generated in code (`MakeRoundRect`, `MakeCircle`, `MakeSoftDot`,
  `MakeRing`, `MakeSpeakerCone`, `MakeSlash`, `MakeVerticalGrade`, `MakeVignette`).
  There are **no external art assets**.
- Button clicks are wired with `UnityEventTools.AddPersistentListener` at build
  time, so handler methods must be `public void` with no arguments.
- **If a script renames a public member, the scene silently keeps the old
  serialized reference** — always re-run Build Scene after refactoring.
- A stale scene plus new scripts is the single most common source of confusing
  runtime errors in this project.

### Canvas geometry — the number everyone gets wrong

`CanvasScaler` reference is **1080×1920** with `matchWidthOrHeight = 0.5`, which
is a *log-weighted* lerp:

```
scale  = 2^lerp(log2(1080/1080), log2(2340/1920), 0.5) = 1.10398
canvas = 1080/1.10398 × 2340/1.10398 = 978 × 2120 reference px
```

**On a real phone the canvas is 978 px wide, not 1080.** Content wider than
~880 px has under 50 px of margin. This has already caused real bugs.

### Anchoring rule

> Two elements whose spatial relationship carries meaning must be anchored to the
> same edge.

Mixing top-, centre- and bottom-anchored children in one vertical stack makes the
group drift apart by ~100 px per anchor boundary on a tall phone. Children of a
**fixed-size parent** (cards, list rows) are exempt — they cannot drift.

---

## 4. Architecture

`Assets/Scripts/` — ~13,000 lines total, 42 runtime scripts. Singletons, wired by
`SceneBuilder` onto one `Managers` GameObject.

### Core loop

| Script | Responsibility |
|---|---|
| `GameManager` | Puzzle generation, answer checking, mode switching. Owns `correctSolution`. |
| `Line` | One matchstick segment. Handles its own tap, three states (Inactive / Active / Selected). |
| `Digit`, `Number` | Compose 7 `Line`s into a digit, digits into a number. |
| `PlusMinus` | The `+`/`−` operator. **One key, one tap** (see §7). |
| `TimerManager` | Countdown in Training; **count-up stopwatch in Arcade** (`max = 0` signals "no limit"). |
| `GUIManager` | Panel routing, login/register forms, HUD. |

### Online

| Script | Responsibility |
|---|---|
| `AuthManager` | Firebase Auth: email, Google, Facebook, **anonymous guest**. |
| `GoogleSignInBridge` / `FacebookSignInBridge` | Native Android sign-in via `AndroidJavaObject`/`AndroidJavaProxy`. **No external Unity plugin.** |
| `FirebaseDBManager` | Realtime DB root, presence, and **server time** via `.info/serverTimeOffset`. |
| `LobbyManager` | Matchmaking queue, invites, friends, **bot fallback after 5 s**. |
| `ArcadeMatchManager` | Match state machine, round arbitration, rematch negotiation. |
| `BotMatchManager` | Fully local simulated opponent. Never touches Firebase. |

### Progression

| Script | Responsibility |
|---|---|
| `PlayerStatsManager` | ELO, W/L, streaks, **coins**. Ranks Bronze→Master. |
| `LeaderboardManager` | Monthly boards: global, by country, friends. |
| `DailyManager` | **Login** streak + 3 rotating daily challenges. |
| `AdManager` | Rewarded-ad seam. Ships a stub; see §9. |
| `ProgressionGUIManager` | Profile / Leaderboard / Daily screens. |

### Presentation

`SafeAreaFitter` (notch), `ScrollablePanel` (overflow), `CanvasOrientationAdapter`,
`EquationLayout` (portrait ↔ landscape), `TapIndicator` (tutorial ripple),
`VolumeControl`, `AudioManager`, `ColorManager`, `GameSettings`.

---

## 5. Firebase data model

```
users/{uid}            displayName, online, lastSeen
presence/{uid}         true (onDisconnect removes)
matchmaking/{uid}      displayName, timestamp, mode, firstTo
matches/{id}           hostUid, players{}, settings{}, scores{}, rounds{}, state,
                       winner, rematch{}, nextMatchId
invites/{uid}/{id}     fromUid, fromName, mode, firstTo, status, matchId
friends/{uid}/{fid}    displayName
stats/{uid}            elo, wins, losses, matches, currentStreak, bestStreak,
                       roundsWon, roundsLost, coins, country, displayName
daily/{uid}            streak, longestStreak, lastLoginDate, streakClaimedDate,
                       challengeDate, challenges{}
leaderboard/global/{yyyy-MM}/{uid}         name, elo, wins, country
leaderboard/country/{CC}/{yyyy-MM}/{uid}   same
```

Rules live at `firebase-database-rules.json` in the repository root, next to this document. `.indexOn: "elo"`
on the leaderboard month nodes is **required** — without it Firebase downloads
every row to the phone and sorts locally, burning the free-tier quota.

**Trust model:** the client writes `stats/{uid}` directly. Rules only check
ownership. There are currently **no `.validate` constraints** — see §10.

---

## 6. Match flow (1v1)

```
RANDOM BATTLE
   → pay entry fee
   → enter matchmaking queue
   → 5 s without a human?  →  BotMatchManager takes over (player is not told)
   → host generates the equation and writes it to Firebase
   → both clients render the same puzzle
   → first correct answer ends the round        ← no timer
   → both see "CORRECT ANSWER: 27 + 41 = 68"
   → first to N rounds wins the match
   → ELO + coins settle
   → REMATCH is negotiated through rematch{} on the finished match node
```

**Round arbitration** is host-side: `ArcadeMatchManager.WatchForFirstCorrect()`
polls the answers node from the moment the round opens and settles on the first
`correct == true`. It must start at round open, not after the host answers —
otherwise a host who never solves deadlocks the round.

**Bots are not disclosed to the player.** They use the same name shapes as real
guests (`Player_48213`) and their skill tracks the player's ELO. This is standard
practice, but it is a deliberate choice and should be revisited if the player base
ever becomes large enough that it is unnecessary.

---

## 7. Design decisions worth preserving

Each of these was a real problem with a non-obvious fix.

**The operator has no invalid state.** It used to be two independently tappable
sticks: four physical states for two meanings, where "neither lit" and "only
vertical lit" could never be correct and `CheckAnswer` failed *silently*. In Hard,
12 of 16 combinations were dead. It is now a single key that toggles between −
and +, framed like every other button so it reads as one control.

**Ranked difficulty is symmetric.** Hard pays ×1.25 rating on a win and costs
×1.25 on a loss. A win-only bonus is the obvious reading of "harder should pay
more", but it inflates the ladder until rating means nothing. **Difficulty is paid
in coins, not rating** — coins have no ladder to distort.

**Bot matches move rating at half K (16 vs 32).** They still count, but they
cannot carry someone up the leaderboard.

**Dates come from the server**, never the device
(`FirebaseDBManager.ServerDateKey`). Streaks cannot be farmed by changing the phone
clock.

**A failed read is not an empty account.** Both `PlayerStatsManager.Load()` and
`DailyManager.Load()` return early on a faulted read instead of saving defaults
over a real record. This was a live data-loss bug.

**Training is free forever.** This is the anti-lockout guarantee. Everything else
in the economy is built on it.

---

## 8. Economy

### Current constants

| Thing | Value | Where |
|---|---|---|
| Starting balance (new account) | **100** | `PlayerStatsManager.START_COINS` |
| Login bonus | `50 + 10×(streak−1)`, cap **150** | `DailyManager.StreakReward` |
| Daily challenges | 3/day, 15–40 each | `DailyManager.POOL` |
| Win payout | **10 / 18 / 30** (Easy/Med/Hard) | `PlayerStatsManager.WinCoins` |
| Entry fee | **5 / 8 / 12** ← *needs changing, see below* | `PlayerStatsManager.EntryFee` |
| Rewarded ad | **50** ← *needs changing* | `ArcadeGUIManager.AD_REWARD` |
| Ad daily cap | 5 | `AdManager.dailyCap` |

### ⚠ Open decision: the entry fee is mathematically broken

An economy audit found the current fees create a **guaranteed slow lockout**.

Break-even win rate is `fee / payout`:

| Mode | Fee | Payout | Break-even | Drift at 50% WR |
|---|---|---|---|---|
| Easy | 5 | 10 | **50.0%** | **0.00** |
| Medium | 8 | 18 | 44.4% | +1.00 |
| Hard | 12 | 30 | 40.0% | +3.00 |

Easy's break-even is *exactly* the equilibrium win rate that ELO matchmaking (and
the ELO-tracking bot) drives every player toward. Expected drift is exactly zero,
forever — a zero-drift random walk against an absorbing barrier at zero hits zero
with probability 1. Over 100 Easy matches, **46% of players end below where they
started**. Easy is also the mode weak players are pushed into, so the fee ladder
punishes exactly the people who most need to keep playing.

**Recommended correction (not yet applied):**

| Mode | Fee | Break-even | Drift at 50% |
|---|---|---|---|
| Easy | **3** | 30.0% | +2.0 |
| Medium | **5** | 27.8% | +4.0 |
| Hard | **9** | 30.0% | +6.0 |

Plus: `AD_REWARD` **50 → 30** (it must stay under the 50-coin day-1 login bonus,
or the ad out-earns the daily hook), a **new-player shield** making ranked entry
free while `Matches < 25`, and **3 free ranked entries per day** when broke.

### The real sink is cosmetics, not entry fees

An entry fee creates tension; it cannot be the economy. `ColorManager` and
`GameSettings` already hold **8 segment colours and 8 background colours, all
free**. Pricing them (leave 2 of each free; ~400–500 per colour, ~1,500 per theme)
turns every coin already being earned into progression, and needs no gate at all.
This is the intended long-term sink and it is greenfield.

### Anti-lockout invariant

> A player can always play, and can always earn coins, without spending money.

Enforced by: Training is free and unlimited (`OnPlayTrainingPressed` on the
out-of-coins sheet); the login bonus arrives regardless of play; and the
out-of-coins sheet never hides its free exit even when no ad can be served.

---

## 9. Ads

`AdManager` is the only seam. **No ad SDK is installed.**

It ships a **stub** so the whole flow is testable today, with two guards against
shipping it:

1. Without the `MATHSTICK_ADS_ADMOB` compile symbol it logs a loud warning on
   every use.
2. In a **non-development build** without the SDK, `IsRewardedReady` returns
   `false` — a released APK with no SDK pays out nothing.

**Contract:** `ShowRewarded(Action<bool>)` calls back `true` **only** on a
completed view. Rewarding a dismissal violates every ad network's policy and gets
accounts suspended.

**To go live:** install the Google Mobile Ads Unity plugin, define
`MATHSTICK_ADS_ADMOB`, and fill in `InitialiseReal` / `IsRealAdLoaded` /
`ShowReal`. Use Google's public **test** ad unit IDs during development — never
click live ads on your own account.

**Two bugs that only appear with a real ad, to fix before integrating:**
`FirebaseDBManager.OnApplicationPause` must early-return while an ad is showing,
or a fullscreen ad removes the presence node and the opponent's 15-second
disconnect grace period forfeits the match.

**Best placement for revenue is not the out-of-coins gate** — with ~150 coins/day
of passive income it will fire for almost nobody. It is a **"double your match
reward"** button on the result screen: opt-in, post-win, emotionally free.

---

## 10. Known gaps

| Area | State |
|---|---|
| **Firebase rules have no `.validate`** | One authenticated `curl` sets `coins = 999999999` and forges a Master rank on the public leaderboard. Highest value-per-effort fix: ~40 lines in one file, deployable from the console, free on Spark. |
| `matches` / `matchmaking` world-writable | Any authenticated user can write any match node. Scope writes to `auth.uid`. |
| `ShowError` never refunds `pendingFee` | An error during matchmaking silently eats the entry fee. |
| Fresh-account arbitrage | A new anonymous account is worth 150 coins. No ad price competes; must be closed by not paying `START_COINS` to accounts that never play, or accepted. |
| Cosmetics shop | Not built. No prices, no ownership, no UI. |
| iOS | Blocked on hardware/account, not code. |
| Rotation testing | **Unity's Device Simulator does not change `Screen.width/height` on Rotate.** Landscape only testable on a real device. |

---

## 11. Business model

**Free to play, ad-supported, cosmetics-monetised.**

| Line | Model |
|---|---|
| Rewarded video | Primary. ~$5–15 eCPM. Opt-in only. |
| Cosmetics | Coins earned in play; optional IAP later. **Never pay-to-win** — advantage-for-money in a maths game kills it. |
| Interstitials | Deliberately absent. Would break the 1v1 race. |

**Costs:** Firebase Spark **free** (100 concurrent connections, 1 GB, 10 GB/month
egress). Google Play developer account **$25 one-time**. AdMob free. The main
cost risk is crossing Spark's connection limit, which is a good problem.

**Because the game may read as child-directed** (it is a maths puzzle), Google
Play families policy and COPPA affect ad personalisation and therefore eCPM. This
must be resolved before monetising.

**Retention design**, in the order it was built and in order of impact:
bot fallback (without it matchmaking dead-ends at zero players) → rematch
(multiplies session length) → stats/ELO/ranks (the number that goes up) → monthly
leaderboards, which **reset** so nobody faces an unreachable all-time top ten →
daily login streak → daily challenges.

---

## 12. Working on this project

```
Unity Hub → Projects → PlusMinus_Unity   (D:\JOC\PlusMinus_Unity\PlusMinus_Unity)
Unity menu → PlusMinus → Build Scene     ← after ANY UI or script-field change
Play ▶
```

**Checklist before believing a UI bug:**

1. Did you re-run **Build Scene**? A stale scene with new scripts is the most
   common cause of confusing failures.
2. Is the Console clear? Errors block everything downstream.
3. Are you assuming a 1080-wide canvas? It is **978**.
4. Are you testing rotation in the Simulator? That does not work.

**Firebase rules** must be published from the console at
`console.firebase.google.com/project/plusminus-46df9/database/plusminus-46df9-default-rtdb/rules`
— copy `firebase-database-rules.json` wholesale, replacing everything.
